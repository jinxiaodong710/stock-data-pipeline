# SMC 小市值策略回测差异分析报告

## 概述

同一份策略代码（`smc_strategy.py`）在 **WSL (Python 3.14)** 和 **Mac (Python 3.9.6)** 两台机器上，用同一份 `stock_data.duckdb` 文件回测，结果差异巨大：

| 指标 | Mac 原始 | WSL | 差值 |
|------|---------|-----|------|
| 年化收益 | **+70.78%** | +50.21% | **+20.57ppt** |
| 最终资产 | **1,759万** | 885万 | +874万 |
| 夏普比率 | **1.58** | 1.13 | +0.45 |
| 最大回撤 | **-40.09%** | -48.25% | -8.16ppt |

这个差异不是由 Python 版本、DuckDB 版本或浮点精度导致的，而是由**数据库中的 ST 历史数据不完整**以及**静态股票名称过滤（`bad` 集）在机器间不一致**共同造成的。

---

## 根因一：stock_st_history 数据不全（主因，贡献~10ppt）

### 现象

Mac 上 `stock_st_history` 表仅有 **7,263行**，而 WSL 上有 **251,383行**。

### 影响

策略的 ST 过滤逻辑：
```python
if (code, dt) in st_set: continue  # 检查某股票在某日是否ST
```

`st_set` 只有 7,263条记录，只能覆盖极少一部分 ST 记录。绝大多数在 2021-2026 年间被标记为 ST 的股票在 Mac 上**没有被识别为 ST**。

以 2021-01-04 为例：
- WSL 正确识别 **215只** ST 股票并过滤
- Mac 仅识别了远少于此数

这些本该被过滤的 ST/垃圾股市值极小，全部挤入选股池前10名。ST 股票价格波动剧烈（经常连续跌停/涨停），被选入后产生了大量异常收益，导致年化虚高 **~10ppt**。

### 修复

从 WSL 导出完整的 stock_st_history（251,383行），使用 COPY CSV 方式导入 Mac 数据库。

### 结果

| 指标 | 修复前 | 修复后 | 差值 |
|------|--------|--------|------|
| 年化 | 70.78% | 60.05% | **-10.73ppt** |
| 最终 | 1,759万 | 1,242万 | -517万 |
| 夏普 | 1.58 | 1.42 | -0.16 |
| 最大回撤 | -40.09% | -47.28% | +7.19ppt |

---

## 根因二：stock_basic_info 中的股票名称未同步（贡献~10ppt）

### 现象

ST 历史修复后，Mac 与 WSL 仍有约 **10ppt 的年化差距**。进一步排查发现两台机器的 `stock_basic_info` 表内容不同。

策略中有一段额外的 ST 过滤逻辑：
```python
bad = set()
for r in con.execute("SELECT ts_code, name FROM stock_basic_info").fetchall():
    if any(k in r[1] for k in ['ST','退','PT']):
        bad.add(r[0][:6])

# 在选股池过滤中
if code in bad: continue
```

这个 `bad` 集**依赖于 `stock_basic_info.name` 的当前值**，即股票的最新名称。问题在于：

- **WSL**：`stock_basic_info` 定期更新，名称较新。例如股票 603729 当前名称为`'龙韵股份'`（已摘帽，ST 标记已移除），只有 8 只股票名称含 ST 但当日未 ST
- **Mac**：`stock_basic_info` 是旧副本，名称未更新。例如 603729 名称仍为`'ST龙韵'`（旧名），合计 **30只** 名称含 ST 但当日未 ST

### 影响

以 603729（龙韵股份）为例：
- 2021-01-11 **不是 ST**
- WSL 的 `bad` 集：`"龙韵股份"` → "ST" not in name → **放行** ✅
- Mac 的 `bad` 集：`"ST龙韵"` → "ST" in name → **误杀** ❌

这导致两台机器的选股池不同，每次选股都有系统性差异，5年累积产生约 ~10ppt 年化差距。

### 修复方案

将静态的 `stock_basic_info.name` 判断替换为基于 `stock_st_history` 的动态判断：

```python
before:
    bad = set()
    for r in con.execute("SELECT ts_code, name FROM stock_basic_info")...
    if code in bad: continue

after:
    ever_st = set()
    for r in con.execute("SELECT DISTINCT ts_code FROM stock_st_history "
                          "WHERE name LIKE '%ST%' OR name LIKE '%退%' OR name LIKE '%PT%'").fetchall():
        ever_st.add(r[0][:6])
    if code in ever_st: continue
```

这样判断标准不再依赖 `stock_basic_info` 的更新状态，两台机器使用同一份 `stock_st_history` 数据，结果完全一致。

---

## 修复后的一致性验证

### 选股池对比（2021-01-04）

两台机器选股池前10名完全一致：

```
Rank  Code      total_mv     close
1     600520    108,207.69   6.83
2     002633    110,100.00   7.34
3     600768    119,436.25   8.93
4     603029    123,115.46   13.19
5     600778    124,285.05   3.99
6     600615    126,161.76   6.71
7     600561    127,161.13   4.47
8     002862    127,440.00   10.62
9     600455    127,476.78   20.41
10    603269    128,946.62   11.46
```

### 交易明细对比

首批10笔交易的股票、价格、股数、资金完全一致。
（2021-01-05 买入 → 2021-01-08 卖出，共3845笔交易的路径完全重合）

### 最终结果

| 指标 | Mac | WSL | 一致性 |
|------|-----|-----|--------|
| 年化 | 51.10% | 51.10% | ✅ 精确一致 |
| 最终 | 9,131,777 | 9,131,777 | ✅ 精确一致 |
| 交易 | 3,845笔 | 3,845笔 | ✅ 精确一致 |
| 胜率 | 55.7% | 55.7% | ✅ 精确一致 |
| 夏普 | 1.33 | 1.33 | ✅ 精确一致 |
| 最大回撤 | -47.76% | -47.76% | ✅ 精确一致 |

---

## 结论

| 原因 | 贡献年化偏差 | 修复方法 |
|------|------------|---------|
| `stock_st_history` 数据不全（7,263 vs 251,383行） | **~10ppt** | 从完整数据库导入CSV |
| `stock_basic_info.name` 机器间不一致 | **~9ppt** | 改用 `stock_st_history` 判断 |
| Python / DuckDB 版本差异 | **~0ppt** | 对纯Python逻辑无影响 |
| 浮点精度累积 | **~0ppt** | 验证无差异 |

最终策略真实年化：**51.10%**（不含手续费）

**经验教训：**
1. `stock_st_history` 必须每日完整采集，缺一天数据就会漏掉当天的全部ST过滤
2. 不要用 `stock_basic_info.name` 做历史ST判断——名称可能早已变更
3. 两台机器之间的副本要保持同步，定期导出/导入完整数据
