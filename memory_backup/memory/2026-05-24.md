# 2026-05-24 (周日)

## 💡 晓东说：涉及记忆的事都最重要
自己建了的备份体系不记得，说明 MEMORY.md 该记的没记。铁律：**做过的设置、写过的文件、搭过的系统，只要和我有关的，都得写进 MEMORY.md，不能只存在文件里。**

## 🧠 今日重要记录

### 1. 内部数据流架构确认
- 用户 → 米娅(OpenClaw/Mac) → 小五(OpenClaw/上海) → 首尔（数据中心）
- 红牛(Hermes/上海) 单独跑飞书bot，不走数据流

### 2. NAS连接信息确认
- 192.168.3.40（UGREEN-1460），用户bigboy，密码Duo710710
- SMB共享已挂载使用
- 已有自动备份体系（米娅备份/目录）

### 3. 内网笔记本
- 192.168.3.35，用户xiaodong，PIN码0099
- 端口全关（SSH/RDP/SMB都关着），暂时无法远程

### 4. 首尔快照API修复
- snapshot_api.py（端口8081）进程运行4天后卡死
- 重启后恢复，加了每30分钟健康检查cron
- API端点: /health, /api/snapshot, /api/ranking, /api/batch

### 5. Mac数据同步问题
- 根因：同步cron在5/23重组时被误删
- 同步脚本(sync_from_seoul.sh) 5/21起SCP一直大小校验失败
- DB文件损坏（1.1G但只有92行）
- 修复：加回cron（1cbd68ac），补了3~5月数据（29万行）

### 6. 头等舱选股脚本
- head_class_selector.py 已部署到首尔(~/go/)和Mac(~/go/)
- 数据库辅助表(stock_fundamentals, stock_sectors)已创建
- 股票名称从快照库补全（5193只）
- Mac上能跑但数据不全（缺2020~2022历史），去掉了新高过滤

## 红牛服务器修复

### 背景
- 晓东说「红牛还不能用」——一开始我忘了红牛是什么（记忆里叫「新服务器」）
- 红牛 = 111.229.0.148（腾讯云国内，OpenCloudOS 9.4）
- 跑 Hermes Gateway + gpt-5.5 (Schyler中转)，连飞书

### 排查过程
1. SSH 上红牛，发现 Hermes 进程活着（PID 734380，01:24启动），但飞书用户发消息得到的是预设回复而非模型回复
2. 查 error.log 发现大量 `Empty response (no content or reasoning) — retry 3/3` — gpt-5.5 返回空内容
3. 查 agent.log 发现 title_generator 收到 HTML：`'<!doctype html>\n<html lang="zh-CN">'`
4. 直接 curl 测试 Schyler API：非流式 ✅ / 流式 ✅ / 长上下文 ✅ — API 本身好着
5. 问题根因：
   - **主因**：Schyler 中转间歇性返回空内容（00:47、01:27、05:44 都出现），不是红牛本身问题
   - **次要**：`config.yaml` 里 `model.base_url` 设为 `https://api.schyler.top`（缺 `/v1`），辅助请求（title生成）打到网站首页返回 HTML，而 `.env` 的 `OPENAI_BASE_URL` 是带 `/v1` 的，主请求正常
6. 修复：`model.base_url` 补上 `/v1` → `https://api.schyler.top/v1`
7. 重启后 systemd 自动拉起，飞书已重连

### 教训
- 红牛=上海Hermes服务器，名字记牢了
- 配置文件里 base_url 路径不一致（config.yaml 缺 /v1，.env 有）容易踩坑
