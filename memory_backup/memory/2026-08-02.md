# 2026-08-02

## 小五模型切换 DeepSeek Flash（上午）
- 晓东下令：把小五也换成 deepseek flash
- 小五 = xiaowu，111.229.134.97，ubuntu，~/.ssh/tencent_cloud（SSH 别名 xiaowu，记录在 ~/.ssh/config）
- 原模型：schyler/gpt-5.5 中转 → **已失效（403）**，导致最近 cron 连续失败 ~20 次
- 切换过程踩坑：只改模型名不行，必须同时补 provider 定义 + auth 配置，否则请求必然失败（小五报错根因）
- 最终：用回小五自己的 DEEPSEEK_API_KEY，模型 deepseek/deepseek-v4-flash，gateway 重启成功，实测通过
- 改了 5 处配置：openclaw.json / systemd 环境变量 / models.json 等（均有备份）
- 「每日服务器巡查」cron 也切到 flash
- 遗留：schyler key 已废，不用再管

## 教训
- 换模型三件套：模型名 + provider 定义 + auth/key，缺一不可
- 6/22 有人把小五配置写成 deepseek 但字段写错位置，一直没生效——要用正规命令写入并验证
