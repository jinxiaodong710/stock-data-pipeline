# 2026-05-23 周六

## 🚨 晓东指令：周末休市不发排行榜

晓东明确说「周末不要发，现在休市」，已记入 MEMORY.md 铁律：

- **周末一律不发送任何股市排行榜/行情推送**
- 盘中实时排行榜严格限制在交易日 9:30-15:00
- 非交易日 cron 触发 → 静默跳过，不发消息

## 🧠 上下文机制学习

晓东详细讲解了上下文怎么传给模型，纠正了我之前的错误理解。

**关键认知纠正：** 不是「所有历史对话全文无脑打包」传给模型，而是「按需重组拼装」。

### 分层结构
| 层级 | 策略 |
|------|------|
| 系统提示 | ✅ 最稳定，几乎必带 |
| 当前会话消息 | ✅ 相关消息带上，太长则压缩 |
| 工具调用结果 | ⚠️ 太长截断，只保留关键部分 |
| 持久记忆 | ✅ 提炼后的长期事实，不是全文 |
| 技能（skill） | 🔸 按需加载，不是全部默认塞 |
| 历史会话 | 🔸 档案库，按需检索，不默认全带 |

### 什么时候会丢内容？
- 上下文太长 → 系统自动压缩成摘要（HEARTBEAT里的手动截断不需要了）
- 工具输出太长 → 截断/分页/摘要
- 话题切换多轮 → 旧话题弱化

### HEARTBEAT.md 旧备注已确认可删除
「上下文超过 500K tokens 时主动截断并 dump」这条是多余的，系统自己会处理。

（晓东哥哥 2026-05-23 10:17 亲授）

## 🛠️ Cron 集中修复（13:12，Pro 下执行）

**问题：** 存档/总结类cron周末也在跑，且推送到微信，导致用户收到股票相关内容

**修改项：**
| cron | 改前 | 改后 |
|------|------|------|
| 对话存档-白天 | 每天 6-22/2h，推微信 | 仅工作日，不推微信 |
| 对话存档-夜间 | 每天 22,2点，推微信 | 仅工作日，不推微信 |
| 对话总结-中午 | 每天 12点，推微信 | 仅工作日，不推微信 |
| 对话总结-午夜 | 每天 0点，推微信(任务内嵌message send) | 仅工作日，删掉message send，不推微信 |
| 每日新闻早报 | 每天 6点，推微信 | 仅工作日，推微信 |
| 盘中实时排行榜 | 工作日 9-15每30分钟 | 工作日 10-15每小时 |

## 🔍 真正元凶：launchd push_monitor（13:19发现）

**问题：** 改完 cron 后，13:19 还是收到股票推送。排查发现真正的始作俑者不在 cron，而是 **launchd 系统级定时任务**，绕过 OpenClaw 直接推微信。

### com.stock.push-monitor
- 脚本：`~/go/push_monitor.sh`（SSH首尔拉 `/tmp/market_monitor.txt`）
- 间隔：原来900秒(15分钟)→ 改为3600秒(1小时)
- 修复：加工作日判断 `WDAY=$(TZ="Asia/Shanghai" date +%u); [ "$WDAY" -gt 5 ] && exit 0`
- 时间判断：10:00-11:30, 13:00-15:05，但没有工作日判断

### com.stock.snapshot
- 脚本：`~/go/snapshot_loop.sh`（30秒快照循环） + `snapshot_sender.py`
- 修复：周末检测放入 while 循环内，sleep 3600 再 continue

### 修复命令
```bash
launchctl unload ~/Library/LaunchAgents/com.stock.push-monitor.plist
launchctl unload ~/Library/LaunchAgents/com.stock.snapshot.plist
# 修改脚本...
launchctl load ...
```

## 🖨️ 打印机配置

### HP Deskjet 3540（已有）
- IPP Everywhere 协议，闲置
- ⚠️ 彩色墨盒空(0%)，黑墨偏低(20%)
- 打印技巧：加 `-o cupsPrintQuality=High -o print-color-mode=monochrome` 让打印更深

### HP DeskJet 2700（新加）
- 通过 Bonjour AirPrint 发现，lpadmin 直接添加
- 驱动：macOS 原生 IPP Everywhere（不需额外装）

### 打印命令
```bash
# 双面打印 + 高质量单色
lp -d HP_Deskjet_3540 -o sides=two-sided-long-edge -o cupsPrintQuality=High -o print-color-mode=monochrome file.pdf
# 取消
cancel 打印机名-作业号
```
