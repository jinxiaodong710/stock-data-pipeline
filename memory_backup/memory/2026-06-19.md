# 2026-06-19 端午节

## 🔧 红牛修复
- 红牛 Hermes gateway 网页端 503 错误
- 根因：auth.json 的 credential pool base_url 指向 `api.schyler.top`，config.yaml 写的是 DeepSeek 直连但被 auth.json 覆盖
- 后来发现 `/root/.hermes/.env` 里 `OPENAI_BASE_URL=https://api.schyler.top/v1` 和 `OPENAI_API_KEY=sk-137…3447` 才是真正覆盖配置的元凶
- 修复：三个地方统一改 DeepSeek 直连配置 + 重启 gateway

## 🔧 小五修复
- 飞书能收到消息但无法回复（`TypeError: Cannot read properties of undefined (reading 'run')`）
- 根因：agent 在用 Schyler API（`schyler/gpt-5.5`），和红牛一样的问题
- 修复：models.json 改 DeepSeek provider、auth-profiles 改 deepseek、config.json default 改 deepseek/deepseek-v4-pro、.env 的 OPENAI_API_KEY 改 DeepSeek 的 key
- 重启 gateway 后 Feishu WebSocket 连接正常、event-dispatch ready

## 💸 API 成本教训
- 今天 DeepSeek 从 ¥31+ → ¥6.56 → ¥1.19，晓东又充了 ¥10 到 ¥6.9
- 总消耗约 ¥20+
- 根因：Pro + Think:high + 183k 上下文未压缩 + 大量 SSH 工具调用输出 → 每轮 ¥0.4-0.5，20+ 轮累积
- OpenClaw 统计成本仅 $0.43，但 DeepSeek 按实际 thinking token + input 计费，未统计项才是大头
- 晓东认为不是 Think 的问题，是上下文太大 + 干活太多
- 最终切回 Flash，成本降低
- **教训：重活儿 spawn 子 agent，不拖主 session**

## 📅 节假日停推送教训（重复犯错！）
- 端午节休市，晓东说别推了
- 我只关了 OpenClaw cron，忘了 launchd（`com.stock.snapshot` / `com.stock.push-monitor`）
- 10:30 又收到推送，晓东很生气
- 已停：launchctl unload 两个任务
- **教训：节假日停推送必须 = cron disabled + launchd unloaded，缺一不可**

## 🧠 模型切换
- 系统默认模型从 Flash 改到 Pro（config 写死了"primary": "deepseek/deepseek-v4-pro"）
- 但晓东最后让切回 Flash 先
- 当前 session: Flash，默认 config 仍为 Pro
